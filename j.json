{
  "result": "success",
  "type": "generate",
  "text": "# convert send_data variable (python data type) into string datatype\nstringified = json.dumps(send_data)\nencoded_data = stringified.encode('ascii')\nprint(type(encoded_data), encoded_data)\n</cell>\n<cell>\n# send data to server\nr = requests.post(url, data = encoded_data, headers=headers)\n\n# print status code\nprint(r.status_code)\n\n# print data\nresult = r.json()\nresult\n</cell>\n<text>\nExercise 03: Sending Data from Local Computer to Server\nWe can send data to a server using the same methodology that we have used in the previous exercise, where we use the requests library (imported as requests), to send a POST request to the server, passing in the url, data, headers, and a cookie (the server uses cookies to maintain login session).\n\nFor this exercise, try sending a JSON string to the server using the same method, just by modifying the data variable in the code cell below. Remember to include the header information and the session cookie.\n\n\n</text>\n<cell>\n# initialize variables\nurl = \"http://cf-courses-data.s3.us.cloud-object-storage.appdomain.cloud/IBM-DA0321EN-SkillsNetwork/datasets/ex1\"\ndata = '{\"sender\": {\"name\": \"Alice\"}, \"recipients\": [{\"name\": \"Bob\"}, {\"name\": \"Claire\"}], \"message\": \"Hello, Alice!\", \"timestamp\": \"1970-01-01T00:00:00.000Z\"}'\nheaders = {\"Content-Type\":\"application/json\"}\ncookie ='session_id={}'.format(session_id)\n\n# convert send_",
  "message": ""
}
