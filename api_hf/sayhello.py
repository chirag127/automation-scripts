import re
import requests


def return_code_snippets(prompt):
    """
    returns a list of code snippets by scraping from the api of the https://beta.sayhello.so/
    :param prompt: the prompt to be used for the prediction
    """
    prompt = prompt + " " + "python"
    # first get the response from the bing api for the given prompt
    # example: https://api.bing.microsoft.com/v7.0/search?q=Set%20a%20response%20cookie%20in%20FastAPI&count=25
    url = (
        "https://api.bing.microsoft.com/v7.0/search?q="
        + prompt
        + "&count=25&offset=0&mkt=en-US&safesearch=Moderate"
    )
    # Ocp-Apim-Subscription-Key: REDACTED_BING_API_KEY
    headers = {"Ocp-Apim-Subscription-Key": "REDACTED_BING_API_KEY"}
    response = requests.get(url, headers=headers)
    print(response.status_code)
    # example response: {"_type": "SearchResponse", "queryContext": {"originalQuery": "Set a response cookie in FastAPI"}, "webPages": {"webSearchUrl": "https:\/\/www.bing.com\/search?q=Set+a+response+cookie+in+FastAPI", "totalEstimatedMatches": 2320000000, "value": [{"id": "https:\/\/api.bing.microsoft.com\/api\/v7\/#WebPages.0", "name": "Response Cookies - FastAPI", "url": "https:\/\/fastapi.tiangolo.com\/advanced\/response-cookies\/", "isFamilyFriendly": true, "displayUrl": "https:\/\/fastapi.tiangolo.com\/advanced\/re
    # make the payload for the https://internalapi.sayhello.so/infer-documents/
    # example payload:{"question":"set a response cookie in fastapi","bing_results":{"_type":"SearchResponse","queryContext":{"originalQuery":"Set a response cookie in FastAPI"},"webPages":{"webSearchUrl":"https://www.bing.com/search?q=Set+a+response+cookie+in+FastAPI","totalEstimatedMatches":2320000000,"value":[{"id":"https://api.bing.microsoft.com/api/v7/#WebPages.0","name":"Response Cookies - FastAPI","url":"https://fastapi.tiangolo.com/advanced/response-cookies/","isFamilyFriendly":true,"displayUrl":"https://fastapi.tiangolo.com/advanced/response-cookies","snippet":"FastAPI will use that temporal response to extract the cookies (also headers and status code), and will put them in the final response that contains the value you returned, filtered by any response_model. You can also declare the Response parameter in dependencies, and set cookies (and headers) in them. Return a Response directly","dateLastCrawled":"2022-07-18T15:00:00.0000000Z","language":"en","isNavigational":false},{"id":"https://api.bing.microsoft.com/api/v7/#WebPages.1","name":"How to set cookies with FastAPI for cross-origin requests","url":"https://stackoverflow.com/questions/64139023/how-to-set-cookies-with-fastapi-for-cross-origin-requests","isFamilyFriendly":true,"displayUrl":"https://stackoverflow.com/questions/64139023/how-to-set-cookies-with-fastapi","snippet":"Setting and reading cookies in FastAPI can be done through the use of the Request class: Setting the cookie refresh_token. from
    # make the payload for the https://internalapi.sayhello.so/infer-documents/
    payload = {"question": prompt, "bing_results": response.json()}
    # make the request to the https://internalapi.sayhello.so/infer-documents/
    response = requests.post(
        "https://internalapi.sayhello.so/infer-documents/", json=payload
    )
    # example response: {"code_snippets":[{"code":"response.set_cookie","url":"https://stackoverflow.com/questions/64139023/how-to-set-cookies-with-fastapi-for-cross-origin-requests","score":0.8513919711112976,"language":"javascript"},{"code":"from fastapi import Response\n\n@app.get('/set')\nasync def setting(response: Response):\n    response.set_cookie(key='refresh_token', value='helloworld', httponly=True)\n    return True\n","url":"https://stackoverflow.com/questions/64139023/how-to-set-cookies-with-fastapi-for-cross-origin-requests","score":0.7476433515548706,"language":"python"},{"code":"from fastapi import FastAPI, Response\n\napp = FastAPI()\n\n\n@app.post(\"/cookie-and-object/\")\ndef create_cookie(response: Response):\n    response.set_cookie(key=\"fakesession\", value=\"fake-cookie-session-value\")\n    return {\"message\": \"Come to the dark side, we have cookies\"}\n","url":"https://stackoverflow.com/questions/64139023/how-to-set-cookies-with-fastapi-for-cross-origin-requests","score":0.7361212372779846,"language":"python"},{"code":"from fastapi import FastAPI\nfrom fastapi.responses import JSONResponse\n\napp = FastAPI()\n\n\n@app.post(\"/cookie/\")\ndef create_cookie():\n    content = {\"message\": \"Come to the dark side, we have cookies\"}\n    response = JSONResponse(content=content)\n    response.set_cookie(key=\"fakesession\", value=\"fake-cookie-session-value\")\n    return response\n","url":"https://fastapi.tiangolo.com/advanced/response-cookies/","score":0.7205557227134705,"language":"python"},{"code":"fastapi.Response","url":"https://fastapi.tiangolo.com/advanced/response-cookies/","score":0.5367353558540344,"language":"go"}],"urls":[],"context":"<P> Response Cookies - FastAPI And then you can set cookies in that temporal response object. from fastapi import FastAPI, Response post(\"/cookie-and-object/\") set_cookie(key=\"fakesession\", value=\"fake-cookie-session-value\") And if you declared a response_model, it will still be used to filter and convert the object you returned. FastAPI will use that temporal response to extract the cookies (also headers and status code), and will put them in the final response that contains the value you returned, filtered by any response_model. You can also declare the Response parameter in dependencies, and set cookies (and headers) in them. Return a Response directly¶ <P> @app. async def setting(response: Response): set_cookie(key='refresh_token', value='helloworld', httponly=True) This is great for sensitive data such as a refresh token. But if your data isn't that sensitive then you can just omit it. Reading the cookie get('/read') Share 3k3333 gold badges8080 silver badges121121 bronze badges add_middleware( <P> @app. Set the cookie by returnrate responses import JSONResponse get(\"/items\") set_cookie(\"test_token\", \"ABC_token\") Detailed parameter They are small files left on the computer by the server setcookie(name,value,expire) -Set cookie . . Cookies . . <P> html This talk can add additional metadata checksum . FastAPI (52) - Response Cookies Response Settings Cookies Cookies and HttpSeesion Cookies cookie ASP. NET implements local refresh, ScriptManager and UpdatePanel controls gz decompression command Daquan js basic usage record programmerall. com","page_names":[],"passages":[{"text":"Response Cookies - FastAPI And then you can set cookies in that temporal response object. from fastapi import FastAPI, Response post(\"/cookie-and-object/\") set_cookie(key=\"fakesession\", value=\"fake-cookie-session-value\") And if you declared a response_model, it will still be used to filter and convert the object you returned. FastAPI will use that temporal response to extract the cookies (also headers and status code), and will put them in the final response that contains the value you returned, filtered by any response_model. You can also declare the Response parameter in dependencies, and set cookies (and headers) in them. Return a Response directly¶","best_sentence":"Response Cookies - FastAPI","score":0.402587890625},{"text":"@app. async def setting(response: Response): set_cookie(key='refresh_token', value='helloworld', httponly=True) This is great for sensitive data such as a refresh token. But if your data isn't that sensitive then you can just omit it. Reading the cookie get('/read') Share 3k3333 gold badges8080 silver badges121121 bronze badges add_middleware(","best_sentence":"async def setting(response: Response):","score":0.20556640625},{"text":"@app. Set the cookie by returnrate responses import JSONResponse get(\"/items\") set_cookie(\"test_token\", \"ABC_token\") Detailed parameter They are small files left on the computer by the server setcookie(name,value,expire) -Set cookie . . Cookies . .","best_sentence":"Set the cookie by returnrate","score":0.1700439453125},{"text":"html This talk can add additional metadata checksum . FastAPI (52) - Response Cookies Response Settings Cookies Cookies and HttpSeesion Cookies cookie ASP. NET implements local refresh, ScriptManager and UpdatePanel controls gz decompression command Daquan js basic usage record programmerall. com","best_sentence":"FastAPI (52) - Response Cookies Response Settings Cookies","score":0.0194244384765625}],"snippets":["FastAPI will use that temporal response to extract the cookies (also headers and status code), and will put them in the final response that contains the value you returned, filtered by any response_model. You can also declare the Response parameter in dependencies, and set cookies (and headers) in them. Return a Response directly","Setting and reading cookies in FastAPI can be done through the use of the Request class: Setting the cookie refresh_token. from fastapi import Response @app.get ('/set') async def setting (response: Response): response.set_cookie (key='refresh_token', value='helloworld', httponly=True) return True. Setting httponly=True makes sure the cookie ...","FastAPI will use this temporary response to extract the cookie (and HEADERS, STATUS_CODE) and put them in the final response containing the return value, by any Response_Model filtering; You can also Dependency Declaring the response parameters and set cookies in it, Headers . Request result . Take a look at Application. Set the cookie success","Delete the cookie from local storage. Open the browser and call the endpoint /cookiefail. Note the cookie is NOT set when the response is a redirect response (check local storage for cookies, it is empty)","Go to a website, right-click check the element; or directly F12 Find Application-cookies to see it. FastAPI offers cookies Cookie is Path and Query's \"Sister\" class It also inherits the same general param class Notice: Import Query, Path, Cookie, etc. from FastApi, these actually Returns a special class function"]}
    # code snippets is the list of code from the response of the request
    if response.ok:
        response_json = response.json()
        code_snippets = response_json["code_snippets"]
        # check if the language is python
        list_of_code_snippets = [
            code_snippet["code"]
            for code_snippet in code_snippets
            if code_snippet["language"] == "python"
        ]
        context = response_json["context"]
    else:
        print(response.text)
        raise Exception("Error in request")
    # make the payload for the request to the api https://internalapi.sayhello.so/infer-answers/
    # payload example: {"question":"set a response cookie in fastapi","context":"<P> Response Cookies - FastAPI And then you can set cookies in that temporal response object. from fastapi import FastAPI, Response post(\"/cookie-and-object/\") set_cookie(key=\"fakesession\", value=\"fake-cookie-session-value\") And if you declared a response_model, it will still be used to filter and convert the object you returned. FastAPI will use that temporal response to extract the cookies (also headers and status code), and will put them in the final response that contains the value you returned, filtered by any response_model. You can also declare the Response parameter in dependencies, and set cookies (and headers) in them. Return a Response directly¶ <P> @app. async def setting(response: Response): set_cookie(key='refresh_token', value='helloworld', httponly=True) This is great for sensitive data such as a refresh token. But if your data isn't that sensitive then you can just omit it. Reading the cookie get('/read') Share 3k3333 gold badges8080 silver badges121121 bronze badges add_middleware( <P> @app. Set the cookie by returnrate responses import JSONResponse get(\"/items\") set_cookie(\"test_token\", \"ABC_token\") Detailed parameter They are small files left on the computer by the server setcookie(name,value,expire) -Set cookie . . Cookies . . <P> html This talk can add additional metadata checksum . FastAPI (52) - Response Cookies Response Settings Cookies Cookies and HttpSeesion Cookies cookie ASP. NET implements local refresh, ScriptManager and UpdatePanel controls gz decompression command Daquan js basic usage record programmerall. com","code_snippets":[{"code":"response.set_cookie","url":"https://stackoverflow.com/questions/64139023/how-to-set-cookies-with-fastapi-for-cross-origin-requests","score":0.8513919711112976,"language":"javascript"},{"code":"from fastapi import Response\n\n@app.get('/set')\nasync def setting(response: Response):\n    response.set_cookie(key='refresh_token', value='helloworld', httponly=True)\n    return True\n","url":"https://stackoverflow.com/questions/64139023/how-to-set-cookies-with-fastapi-for-cross-origin-requests","score":0.7476433515548706,"language":"python"},{"code":"from fastapi import FastAPI, Response\n\napp = FastAPI()\n\n\n@app.post(\"/cookie-and-object/\")\ndef create_cookie(response: Response):\n    response.set_cookie(key=\"fakesession\", value=\"fake-cookie-session-value\")\n    return {\"message\": \"Come to the dark side, we have cookies\"}\n","url":"https://stackoverflow.com/questions/64139023/how-to-set-cookies-with-fastapi-for-cross-origin-requests","score":0.7361212372779846,"language":"python"},{"code":"from fastapi import FastAPI\nfrom fastapi.responses import JSONResponse\n\napp = FastAPI()\n\n\n@app.post(\"/cookie/\")\ndef create_cookie():\n    content = {\"message\": \"Come to the dark side, we have cookies\"}\n    response = JSONResponse(content=content)\n    response.set_cookie(key=\"fakesession\", value=\"fake-c
    # make the request to the api
    payload = {"question": prompt, "context": context}
    response = requests.post(
        "https://internalapi.sayhello.so/infer-answers/", json=payload
    )
    # check if the response is ok
    if response.ok:
        # get the response json
        response_json = response.json()
        answer = response_json["answer"]
        # print the answer
        print(answer)
        # attach


if __name__ == "__main__":
    print(return_code_snippets("set a response cookie in fastapi"))
